#!/usr/local/bin/bash
#set -x
if [ -n "$SHLVL" ];then
  i=$(uname)
  if [ $i = Linux -o ${i::6} = CYGWIN -o $i = OpenBSD ];then
    i=$(expr $SHLVL - 1)
  else
    i=$SHLVL
  fi
  echo "At shell level $i"
else
  echo '$SHLVL not set'
fi
